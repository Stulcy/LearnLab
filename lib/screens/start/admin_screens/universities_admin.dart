// 🐦 Flutter imports:
import 'package:flutter/material.dart';

class AdminUniversitiesBody extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return SingleChildScrollView(
      child: Container(
        color: Colors.white,
        margin: const EdgeInsets.all(10),
      ),
    );
  }
}
